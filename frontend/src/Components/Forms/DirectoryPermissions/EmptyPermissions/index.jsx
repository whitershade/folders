import React from 'react';
import styles from './styles.module.css';

const EmptyPermissions = () => (
  <div className={styles.emptyPermissions}>
    Click &quot;Add new&quot; button to set permission
  </div>
);

export default EmptyPermissions;
